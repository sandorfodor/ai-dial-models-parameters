# Work with different LLMs and parameters

Create .env based on the .env.example in the task folder.

Update `run.py` based on which task you want to execute.

### Build & Run

```bash
 docker build -t python-docker .;docker run -it --rm python-docker
```
